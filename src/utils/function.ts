export const warn = (
  msg?: string,
  msgType?: "success" | "warning" | "info" | "error"
) => {
//   ElMessage({
//     message: msg || "接口请求出错，请稍后再试...",
//     type: msgType || "error",
//     grouping: true,
//   });
};
